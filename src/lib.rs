//! `market_protocol` is the generated Rust view of the canonical Protobuf schema.
//!
//! The schema at `proto/market_protocol/v1/market_protocol.proto` is the only
//! protocol model. Rust and Python consumers use generated types directly.

pub mod protobuf;
pub use protobuf::v1;
